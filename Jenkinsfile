pipeline {
    agent any
    stages {
        stage('Submit Stack') {
            steps {
            sh "aws cloudformation create --template-file monthe.yml --stack-name patrick"
              }
             }
            }
            }
